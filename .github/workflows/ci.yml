name: Test and build macOS
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  macos:
    runs-on: macos-14
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run test:extensions
      - run: npm run dist:mac
      - name: Verify local app signature
        run: codesign --verify --deep --strict 'release/local/mac-arm64/Browser Nova.app'
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: Browser-Nova-macOS-local
          path: |
            release/local/*.dmg
            release/local/*.zip
            release/local/*.blockmap
            release/local/SHA256SUMS
          if-no-files-found: error
          retention-days: 14
